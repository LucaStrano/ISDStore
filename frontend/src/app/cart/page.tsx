export default function CartPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">Cart</h1>
      <p>Your cart items will appear here (to be fetched from Redis via backend).</p>
    </div>
  );
}
